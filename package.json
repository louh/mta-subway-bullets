{
  "name": "mta-subway-bullets",
  "version": "0.5.1",
  "description": "SVG icons based on NYC subway bullets",
  "scripts": {
    "start": "npm run build",
    "build": "gulp"
  },
  "files": [
    "build",
    "svg"
  ],
  "devDependencies": {
    "gulp": "4.0.2",
    "gulp-svg-sprite": "1.5.0"
  }
}
